<IfModule mod_rewrite.c>
    RewriteEngine On

    # Force HTTPS + www en une seule redirection 301
    RewriteCond %{HTTPS} off [OR]
    RewriteCond %{HTTP_HOST} !^www\. [NC]
    RewriteCond %{HTTP_HOST} ^(?:www\.)?(.+)$ [NC]
    RewriteRule ^ https://www.%1%{REQUEST_URI} [L,R=301]
</IfModule>
